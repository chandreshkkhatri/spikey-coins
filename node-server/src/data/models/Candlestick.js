/**
 * @typedef {object} Candlestick
 * @property {string} symbol - The trading symbol (e.g., "btcusdt").
 * @property {string} interval - The candlestick interval (e.g., "5m", "1h").
 * @property {number} openTime - Timestamp of the open time.
 * @property {number} closeTime - Timestamp of the close time.
 * @property {string} open - Opening price.
 * @property {string} high - Highest price.
 * @property {string} low - Lowest price.
 * @property {string} close - Closing price.
 * @property {string} volume - Trading volume.
 */

// This file primarily serves as a type definition for JSDoc.
// No actual class or object is exported unless complex logic is needed for the model itself.
